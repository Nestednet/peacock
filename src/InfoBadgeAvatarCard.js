import * as React from 'react'
import PropTypes from 'prop-types'

import Avatar from '../Avatar'
import Badge from '../Badge'
import Box from './Box.js'
import Icon from '../Icon'
import Text from '../Text'

type Props = {|

|};

// FIXME: Abstreir la classe / Generalitzar

export default class InfoBadgeAvatarCard extends React.Component<Props,State> {
    static propTypes={

    }

    render() {
      const {

      } = this.props
      
      
      return (
        <div>ok</div>
      );
    }
}
